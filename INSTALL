SBLIM Base Instrumentation INSTALL


* Make process :

You can use the contained makefiles to build the complete module . 
It might be necessary for you to modify the setting.cmpi file to
match your system. 

It is essential to make the libCMPIOpenCimom.so ( shared library 
of the CMPI ) accessable via your path descriptions. 

* Please install the module in the following order :

	- make
	- make install
	- make clean

  and load the Linux_Base.mof file into the repository

Have fun. 
